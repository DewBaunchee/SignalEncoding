package by.varyvoda.sigenc.domain.signal.encoder;

import by.varyvoda.sigenc.domain.signal.Signal;

public class SignalEncoderImpl implements SignalEncoder {

    @Override
    public Signal encode(Signal carrier, Signal data) {
        return null;
    }
}
